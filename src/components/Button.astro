---
export interface Props {
  text: string;
}

const { text } = Astro.props;
---

<button
  class="bg-gray-300 hover:bg-gray-400 text-gray-700 hover:text-gray-800 font-bold w-fit px-4 py-2 rounded-lg mx-auto"
  >{text}</button
>
